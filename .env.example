# Database configuration for Battlefield 2042 stats
# You can also use DATABASE_URL instead of individual components

# PostgreSQL connection components
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=bf2042_stats
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# Alternative: Single database URL (comment out the above if using this)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/bf2042_stats
